import React from "react";

export default function Dashboard() {
  return (
    <div className="hero">
      <section className="card">
        <h1>Dashboard</h1>
        <p className="subtle">You are logged in.</p>

        <p className="subtle">Next step: create profiles (work / private / university).</p>
      </section>
    </div>
  );
}
